并发三要素：
    1、可见性：一个线程对共享变量的修改，另外一个线程能够立刻看到。
    2、原子性：即一个操作或者多个操作，要么全部执行并且执行的过程不会被任何因素打断，要么就都不执行。
    3、有序性：即程序执行的顺序按照代码的先后顺序执行。


    Java提供了volatile关键字来保证可见性，通过synchronized和Lock也能够保证可见性，synchronized和Lock可以保证同一时刻，
    只有一个线程获取锁然后执行同步代码，并且在锁释放前会将对变量的修改刷新到主存中，由此可以保证可见性。